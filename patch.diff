diff --git a/src/components/Dashboard.jsx b/src/components/Dashboard.jsx
index a091378..ca88584 100644
--- a/src/components/Dashboard.jsx
@@ -1,6 +1,7 @@
 import React, { useState } from 'react';
 import { useSpendingData } from '../hooks/useSpendingData';
 import { SpendingChart } from './SpendingChart';
+import { RecentTransactions } from './RecentTransactions';
 import { CreditCard, TrendingUp, TrendingDown, DollarSign } from 'lucide-react';
 import { cn } from '../lib/utils';
 import { motion } from 'framer-motion';
@@ -103,6 +104,11 @@ export const Dashboard = () => {
           </div>
         </div>
 
+        {/* Recent Transactions Table */}
+        <div className="mt-8">
+          <RecentTransactions transactions={data.transactions || []} />
+        </div>
+
       </div>
     </div>
   );
diff --git a/src/components/Dashboard.test.jsx b/src/components/Dashboard.test.jsx
index b1da6be..11ae41c 100644
--- a/src/components/Dashboard.test.jsx
@@ -2,7 +2,6 @@ import { render, screen, fireEvent } from '@testing-library/react';
 import { describe, it, expect, vi } from 'vitest';
 import { Dashboard } from './Dashboard';
 import { SpendingChart } from './SpendingChart';
-import { BrowserRouter } from 'react-router-dom';
 import App from '../App';
 
 // Mock Recharts per instructions
@@ -64,8 +63,15 @@ describe('Dashboard Component', () => {
 
   it('renders category breakdown correctly', () => {
      render(<Dashboard />);
-     expect(screen.getByText('food')).toBeInTheDocument();
-     expect(screen.getByText('transport')).toBeInTheDocument();
+     expect(screen.getAllByText('food')[0]).toBeInTheDocument();
+     expect(screen.getAllByText('transport')[0]).toBeInTheDocument();
+  });
+
+  it('renders recent transactions table correctly', () => {
+     render(<Dashboard />);
+     expect(screen.getByText('Recent Transactions')).toBeInTheDocument();
+     expect(screen.getByText('Description')).toBeInTheDocument();
+     expect(screen.getAllByText('Groceries')[0]).toBeInTheDocument(); // from mock data
   });
 });
 
diff --git a/src/components/RecentTransactions.jsx b/src/components/RecentTransactions.jsx
new file mode 100644
index 0000000..8878c0b
--- /dev/null
@@ -0,0 +1,55 @@
+import React from 'react';
+import { cn } from '../lib/utils';
+
+export const RecentTransactions = ({ transactions }) => {
+  return (
+    <div className="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
+      <div className="px-6 py-4 border-b border-gray-200">
+        <h2 className="text-lg font-semibold text-gray-800">Recent Transactions</h2>
+      </div>
+      <div className="overflow-x-auto">
+        <table className="w-full text-sm text-left text-gray-500">
+          <thead className="text-xs text-gray-700 uppercase bg-gray-50 border-b border-gray-200">
+            <tr>
+              <th scope="col" className="px-6 py-3 font-medium">Date</th>
+              <th scope="col" className="px-6 py-3 font-medium">Description</th>
+              <th scope="col" className="px-6 py-3 font-medium">Category</th>
+              <th scope="col" className="px-6 py-3 font-medium text-right">Amount</th>
+            </tr>
+          </thead>
+          <tbody>
+            {transactions.length === 0 ? (
+              <tr>
+                <td colSpan={4} className="px-6 py-8 text-center text-gray-500">
+                  No recent transactions found.
+                </td>
+              </tr>
+            ) : (
+              transactions.map((tx) => (
+                <tr key={tx.id} className="bg-white border-b border-gray-100 last:border-0 hover:bg-gray-50 transition-colors">
+                  <td className="px-6 py-4 whitespace-nowrap text-gray-600">
+                    {new Date(tx.date).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })}
+                  </td>
+                  <td className="px-6 py-4 font-medium text-gray-900">
+                    {tx.description}
+                  </td>
+                  <td className="px-6 py-4">
+                    <span className="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800 capitalize">
+                      {tx.category}
+                    </span>
+                  </td>
+                  <td className={cn(
+                    "px-6 py-4 text-right font-medium whitespace-nowrap",
+                    tx.amount > 0 ? "text-emerald-600" : "text-gray-900"
+                  )}>
+                    {tx.amount > 0 ? '+' : ''}${Math.abs(tx.amount).toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}
+                  </td>
+                </tr>
+              ))
+            )}
+          </tbody>
+        </table>
+      </div>
+    </div>
+  );
+};
diff --git a/src/hooks/useSpendingData.js b/src/hooks/useSpendingData.js
index a6b7496..c0eaffe 100644
--- a/src/hooks/useSpendingData.js
@@ -1,4 +1,4 @@
-import { useState, useMemo } from 'react';
+import { useMemo } from 'react';
 import { mockTransactions } from '../lib/mockData';
 
 export const useSpendingData = (filter) => {
@@ -64,8 +64,10 @@ export const useSpendingData = (filter) => {
       color: categoryColors[name] || categoryColors.other
     })).sort((a, b) => b.value - a.value);
 
+    const sortedTransactions = [...filteredTransactions].sort((a, b) => new Date(b.date) - new Date(a.date));
+
     return {
-      data: { trendData, categoryData },
+      data: { trendData, categoryData, transactions: sortedTransactions },
       metrics: calculatedMetrics
     };
   }, [filter]);