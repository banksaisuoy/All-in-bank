diff --git a/src/components/TransferForm.jsx b/src/components/TransferForm.jsx
index 7cf4539..4f99927 100644
--- a/src/components/TransferForm.jsx
@@ -50,6 +50,17 @@ export const TransferForm = () => {
       return;
     }
 
+
+    if (formData.description && formData.description.length > 200) {
+      setError('Description must be 200 characters or less');
+      return;
+    }
+
+    if (formData.description && /[<>]/.test(formData.description)) {
+      setError('Description contains invalid characters');
+      return;
+    }
+
     if (!formData.pin || formData.pin.length < 4) {
       setError('Please enter a valid PIN');
       return;